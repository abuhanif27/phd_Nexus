# Admin customization

Branding in `nexuscare/urls.py`: `admin.site.site_header`, `site_title`, `index_title` set to PhD NexusCare.

Per-app admin in `apps/<app>/admin.py`: custom list_display, fieldsets, filters. See each app’s `admin.py` for details.
