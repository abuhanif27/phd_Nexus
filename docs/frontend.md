# Frontend

Next.js 15 + React 19 + TypeScript in `frontend/`.

**Run:** `cd frontend && npm install && npm run dev` → http://localhost:3000

Stack: Tailwind, Shadcn/UI, TanStack Query, Zustand, JWT auth. See `frontend/README.md` for details.
