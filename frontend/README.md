# Frontend

Next.js 15, React 19, TypeScript. Tailwind, Shadcn/UI, TanStack Query, Zustand. JWT auth to Django API.

```bash
npm install
npm run dev   # http://localhost:3000
```

Set `NEXT_PUBLIC_API_URL=http://localhost:8000/api` in `.env.local` if needed. See root [SETUP.md](../SETUP.md).
